# Tests placeholder

计划测试层级：unit、contract、integration、fault-injection、upgrade/rollback、Ubuntu 22.04/24.04 matrix。

所有测试证据必须关联 Task/Result；需要进入仓库时由 GIT-SCM 记录对应 commit。Git bootstrap 自身还需验证：身份为 repo-local、remote 脱敏、push 后远端 SHA 一致、工作树状态可解释。
